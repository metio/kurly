---
title: Installation
description: Running kurly workloads on a real cluster — what an operator has to decide and what a release asks of them.
---

kurly is a library, so there is nothing to install: a consumer imports it and
renders. What an operator does have to decide is what the cluster around those
manifests provides.

- **[Upgrading](/installation/upgrading/)** — what a release asks of you before
  the next render lands.
