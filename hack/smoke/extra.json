{
  "aastro": {
    "params": "flows=[{ path: '/smoke', method: 'GET', aggregation: { strategy: 'array', best_effort: false }, upstreams: [{ name: 'admin', hosts: 'http://127.0.0.1:7806', path: '/health', method: 'GET', timeout: '3s' }] }]"
  },
  "baikal": {
    "dbEngine": "mysql"
  },
  "cloudflare-tunnel-ingress-controller": {
    "skip": "The controller reads CLOUDFLARE_API_TOKEN from a Secret and needs a real token, issued in a real Cloudflare account, against a zone somebody owns \u2014 it dials Cloudflare on its first reconcile and exits when the account or the token is not there. Nothing can generate that, so a scenario would either fail on a missing Secret or fail authenticating with a made-up one. What it renders is checked by the render gates."
  },
  "cobalt": {
    "compose": "+ k.env({ API_URL: 'http://cobalt:9000/' })"
  },
  "continuwuity": {
    "params": "serverName='matrix.example.com'"
  },
  "docassemble": {
    "rolloutTimeout": 900
  },
  "docmost": {
    "compose": "+ k.env({ APP_URL: 'http://docmost:3000' })"
  },
  "endurain": {
    "compose": "+ k.env({ ENDURAIN_HOST: 'http://endurain:8080' })"
  },
  "fider": {
    "compose": "+ k.env({ BASE_URL: 'http://fider:3000' })"
  },
  "gotosocial": {
    "compose": "+ k.env({ GTS_HOST: 'social.example.com' })"
  },
  "gravity": {
    "skip": "Gravity runs on the node's network \u2014 DNS, DHCP and TFTP are the point, so the ports it opens ARE the node's ports \u2014 and its embedded etcd binds :2380 there. A Kubernetes control-plane node already has etcd on that port, so on a single-node test cluster the two collide and gravity exits with 'address already in use'. That is a collision with where the test runs rather than a defect in the stage: a real deployment puts it on a node that is not a control plane."
  },
  "habitat": {
    "compose": "+ k.env({ DATABASE_URL: 'postgresql://habitat:Kurly-e2e-Passw0rd@habitat-db-rw:5432/habitat?serverVersion=17&sslmode=disable' })"
  },
  "hatsu": {
    "compose": "+ k.env({ HATSU_DOMAIN: 'hatsu.example.com', HATSU_PRIMARY_ACCOUNT: 'example.com' })",
    "skip": "Hatsu fetches https://<primaryAccount>/ at start-up to discover the site's JSON feed and exits when it cannot, so it needs a real site already published on the public internet \u2014 an in-cluster feed over plain HTTP clears the discovery error and then fails the HTTPS fetch."
  },
  "isso": {
    "params": "host='http://isso:8080'"
  },
  "kubetail": {
    "skip": "kubetail's cluster-api REQUIRES TLS material and validates it before doing anything else: it exits naming five fields at once \u2014 its own certificate and key, and the certificate, key and CA it presents to the cluster-agent. Configuring the sections with tls disabled does not satisfy that validation, and kurly mints no key material, so there is no default render that boots. The cluster-agent and the dashboard start; the api is what a scenario cannot bring up without certificates somebody issued."
  },
  "leantime": {
    "dbEngine": "mysql"
  },
  "libredesk": {
    "compose": "+ k.env({ LIBREDESK_APP__ROOT_URL: 'http://libredesk:9000' })"
  },
  "loki/server": {
    "params": "storageSecret='loki-storage', storageClass='standard'"
  },
  "lychee": {
    "compose": "+ k.env({ APP_URL: 'http://lychee:8000', APP_ENV: 'production', FRANKENPHP_NUM_THREADS: '2' })"
  },
  "mastodon": {
    "maxRestarts": 8,
    "rolloutTimeout": 900
  },
  "matrix-alertmanager-receiver": {
    "params": "homeserverUrl='https://matrix.example.com', userId='@alerts:matrix.example.com'"
  },
  "matrix-conduit": {
    "compose": "+ k.env({ CONDUIT_SERVER_NAME: 'matrix.example.com' })"
  },
  "metrics-server/server": {
    "params": "kubeletInsecureTLS=true"
  },
  "netbox": {
    "rolloutTimeout": 900
  },
  "opencost/server": {
    "params": "prometheusEndpoint='http://prometheus:9090'"
  },
  "oxicloud": {
    "skip": "The published image binds 127.0.0.1: OxiCloud starts, migrates its database and then listens on loopback, where nothing outside the pod can reach it. The binary carries no bind-address option and ignores HOST, SERVER_HOST and OXICLOUD_HOST, all three measured on a live cluster. No manifest can fix a server that will not listen on an address its Service can route to."
  },
  "peertube": {
    "compose": "+ k.env({ PEERTUBE_WEBSERVER_HOSTNAME: 'peertube.example.com' })"
  },
  "pelican-panel": {
    "params": "secretName='pelican-panel'",
    "rolloutTimeout": 900
  },
  "piwigo": {
    "dbEngine": "mysql"
  },
  "prestashop": {
    "compose": "+ k.env({ PS_INSTALL_AUTO: '1', PS_DOMAIN: 'prestashop', PS_HANDLE_DYNAMIC_DOMAIN: '0', PS_ERASE_DB: '0' })",
    "dbEngine": "mysql"
  },
  "remark42": {
    "compose": "+ k.env({ REMARK_URL: 'http://remark42:8080' })"
  },
  "safebucket": {
    "compose": "+ k.env({ DATABASE__POSTGRES__SSLMODE: 'disable', STORAGE__S3__ENDPOINT: 'seaweedfs-headless:8333' })"
  },
  "scraparr": {
    "skip": "Scraparr is an exporter for Sonarr, Radarr, Prowlarr and the rest, and it exits when its configuration names none of them \u2014 an exporter with no target has nothing to scrape, so there is no default render that boots. A scenario would have to stand up one of those applications first, which is a fixture for somebody else's workload rather than a check of this one."
  },
  "seaweedfs": {
    "stageOrder": "master volume filer server"
  },
  "sglang": {
    "skip": "SGLang requires an NVIDIA GPU \u2014 the image is built on CUDA with NVIDIA's entrypoint and the pod requests nvidia.com/gpu, so on a node without the device plugin it stays Pending. The image is also 14 GB compressed, which is more than the disk a throwaway test node has. Deployable on a GPU node and not verifiable here."
  },
  "shlink": {
    "compose": "+ k.env({ SKIP_INITIAL_GEOLITE_DOWNLOAD: 'true' })"
  },
  "speedtest-tracker": {
    "compose": "+ k.env({ APP_URL: 'http://speedtest-tracker' })"
  },
  "superset": {
    "skip": "The published apache/superset image ships no database driver: it exits with ModuleNotFoundError: No module named 'psycopg2' before it reads any configuration, and the read-only root filesystem means nothing can install one at start-up. Upstream's answer is an image derived from theirs with the drivers a deployment needs, which is a build kurly does not do. The stage renders correctly and the image cannot connect to the database the stage declares."
  },
  "synapse/server": {
    "params": "serverName='synapse.example.com'"
  },
  "triton-inference-server": {
    "skip": "The image is eight gigabytes compressed \u2014 more disk than a throwaway test node has \u2014 and it is NVIDIA's CUDA base whether or not a GPU is requested. The stage boots with an empty model repository by design, so what a scenario would prove is that a very large image starts, at the cost of the node's disk."
  },
  "tube-archivist": {
    "compose": "+ k.env({ TA_HOST: 'http://tube-archivist:8000' })"
  },
  "tuwunel": {
    "params": "serverName='matrix.example.com'"
  },
  "varnish": {
    "params": "backendHost='kubernetes.default.svc', backendPort=443"
  },
  "vikunja": {
    "compose": "+ k.env({ VIKUNJA_SERVICE_PUBLICURL: 'http://vikunja:3456' })"
  },
  "wekan": {
    "params": "rootUrl='http://wekan:8080'"
  },
  "wetty": {
    "params": "sshHost='example.internal'"
  },
  "wordpress": {
    "dbEngine": "mysql"
  },
  "your-spotify": {
    "compose": "+ k.env({ API_ENDPOINT: 'http://your-spotify:8080', CLIENT_ENDPOINT: 'http://your-spotify:3000' })"
  },
  "yourls": {
    "compose": "+ k.env({ YOURLS_SITE: 'http://yourls' })"
  }
}
