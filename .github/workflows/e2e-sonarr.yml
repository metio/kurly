# SPDX-FileCopyrightText: The kurly Authors
# SPDX-License-Identifier: 0BSD

# generated by scripts/gen-smoke.sh — do not edit; regenerate with: gen-smoke
# Per-workload e2e for sonarr. Self-gates on a diff scoped to this workload (plus the
# shared library and the e2e machinery), delegating the matrix + run to the
# reusable e2e workflow.
name: E2E sonarr
on:
  pull_request:
    branches: [main]
    paths:
      - 'workloads/sonarr/**'
  workflow_dispatch:
permissions:
  contents: read
concurrency:
  group: e2e-sonarr-${{ github.ref }}
  cancel-in-progress: true
jobs:
  e2e:
    uses: ./.github/workflows/e2e.yml
    with:
      workload: sonarr
      paths: '^workloads/sonarr/'
      scenario: hack/smoke/scenario-sonarr.sh
      max_versions: '1'
