# SPDX-FileCopyrightText: The kurly Authors
# SPDX-License-Identifier: 0BSD

# generated by scripts/gen-smoke.sh — do not edit; regenerate with: gen-smoke
# Per-workload e2e for plex. Self-gates on a diff scoped to this workload (plus the
# shared library and the e2e machinery), delegating the matrix + run to the
# reusable e2e workflow.
name: E2E plex
on:
  pull_request:
    branches: [main]
    paths:
      - 'workloads/plex/**'
  workflow_dispatch:
permissions:
  contents: read
concurrency:
  group: e2e-plex-${{ github.ref }}
  cancel-in-progress: true
jobs:
  e2e:
    uses: ./.github/workflows/e2e.yml
    with:
      workload: plex
      paths: '^workloads/plex/'
      scenario: hack/smoke/scenario-plex.sh
      max_versions: '1'
