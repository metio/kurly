# SPDX-FileCopyrightText: The kurly Authors
# SPDX-License-Identifier: 0BSD

# generated by scripts/gen-smoke.sh — do not edit; regenerate with: gen-smoke
# Per-workload e2e for ghost. Self-gates on a diff scoped to this workload (plus the
# shared library and the e2e machinery), delegating the matrix + run to the
# reusable e2e workflow.
name: E2E ghost
on:
  pull_request:
    branches: [main]
    paths:
      - 'workloads/ghost/**'
  workflow_dispatch:
permissions:
  contents: read
concurrency:
  group: e2e-ghost-${{ github.ref }}
  cancel-in-progress: true
jobs:
  e2e:
    uses: ./.github/workflows/e2e.yml
    with:
      workload: ghost
      paths: '^workloads/ghost/'
      scenario: hack/smoke/scenario-ghost.sh
      max_versions: '1'
