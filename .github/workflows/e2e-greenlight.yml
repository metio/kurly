# SPDX-FileCopyrightText: The kurly Authors
# SPDX-License-Identifier: 0BSD

# generated by scripts/gen-smoke.sh — do not edit; regenerate with: gen-smoke
# Per-workload e2e for greenlight. Self-gates on a diff scoped to this workload (plus the
# shared library and the e2e machinery), delegating the matrix + run to the
# reusable e2e workflow.
name: E2E greenlight
on:
  pull_request:
    branches: [main]
  workflow_dispatch:
permissions:
  contents: read
jobs:
  e2e:
    uses: ./.github/workflows/e2e.yml
    with:
      workload: greenlight
      # yamllint disable-line rule:line-length
      paths: '^(workloads/greenlight/|lib/|main\.libsonnet|hack/smoke/(lib|scenario-greenlight)\.sh|\.github/workflows/e2e(-greenlight)?\.yml)'
      scenario: hack/smoke/scenario-greenlight.sh
      max_versions: '1'
