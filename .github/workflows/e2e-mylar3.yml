# SPDX-FileCopyrightText: The kurly Authors
# SPDX-License-Identifier: 0BSD

# generated by scripts/gen-smoke.sh — do not edit; regenerate with: gen-smoke
# Per-workload e2e for mylar3. Self-gates on a diff scoped to this workload (plus the
# shared library and the e2e machinery), delegating the matrix + run to the
# reusable e2e workflow.
name: E2E mylar3
on:
  pull_request:
    branches: [main]
  workflow_dispatch:
permissions:
  contents: read
jobs:
  e2e:
    uses: ./.github/workflows/e2e.yml
    with:
      workload: mylar3
      # yamllint disable-line rule:line-length
      paths: '^(workloads/mylar3/|lib/|main\.libsonnet|hack/smoke/(lib|scenario-mylar3)\.sh|\.github/workflows/e2e(-mylar3)?\.yml)'
      scenario: hack/smoke/scenario-mylar3.sh
