# SPDX-FileCopyrightText: The kurly Authors
# SPDX-License-Identifier: 0BSD

# generated by scripts/gen-smoke.sh — do not edit; regenerate with: gen-smoke
# Per-workload e2e for oauth2-proxy. Self-gates on a diff scoped to this workload (plus the
# shared library and the e2e machinery), delegating the matrix + run to the
# reusable e2e workflow.
name: E2E oauth2-proxy
on:
  pull_request:
    branches: [main]
  workflow_dispatch:
permissions:
  contents: read
jobs:
  e2e:
    uses: ./.github/workflows/e2e.yml
    with:
      workload: oauth2-proxy
      # yamllint disable-line rule:line-length
      paths: '^(workloads/oauth2-proxy/|lib/|main\.libsonnet|hack/smoke/(lib|scenario-oauth2-proxy)\.sh|\.github/workflows/e2e(-oauth2-proxy)?\.yml)'
      scenario: hack/smoke/scenario-oauth2-proxy.sh
      max_versions: '1'
