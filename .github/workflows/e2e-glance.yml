# SPDX-FileCopyrightText: The kurly Authors
# SPDX-License-Identifier: 0BSD

# generated by scripts/gen-smoke.sh — do not edit; regenerate with: gen-smoke
# Per-workload e2e for glance. Self-gates on a diff scoped to this workload (plus the
# shared library and the e2e machinery), delegating the matrix + run to the
# reusable e2e workflow.
name: E2E glance
on:
  pull_request:
    branches: [main]
  workflow_dispatch:
permissions:
  contents: read
jobs:
  e2e:
    uses: ./.github/workflows/e2e.yml
    with:
      workload: glance
      # yamllint disable-line rule:line-length
      paths: '^(workloads/glance/|lib/|main\.libsonnet|hack/smoke/(lib|scenario-glance)\.sh|\.github/workflows/e2e(-glance)?\.yml)'
      scenario: hack/smoke/scenario-glance.sh
      max_versions: '1'
