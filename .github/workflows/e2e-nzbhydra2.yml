# SPDX-FileCopyrightText: The kurly Authors
# SPDX-License-Identifier: 0BSD

# generated by scripts/gen-smoke.sh — do not edit; regenerate with: gen-smoke
# Per-workload e2e for nzbhydra2. Self-gates on a diff scoped to this workload (plus the
# shared library and the e2e machinery), delegating the matrix + run to the
# reusable e2e workflow.
name: E2E nzbhydra2
on:
  pull_request:
    branches: [main]
    paths:
      - 'workloads/nzbhydra2/**'
  workflow_dispatch:
permissions:
  contents: read
concurrency:
  group: e2e-nzbhydra2-${{ github.ref }}
  cancel-in-progress: true
jobs:
  e2e:
    uses: ./.github/workflows/e2e.yml
    with:
      workload: nzbhydra2
      paths: '^workloads/nzbhydra2/'
      scenario: hack/smoke/scenario-nzbhydra2.sh
      max_versions: '1'
