# SPDX-FileCopyrightText: The kurly Authors
# SPDX-License-Identifier: 0BSD

# generated by scripts/gen-smoke.sh — do not edit; regenerate with: gen-smoke
# Per-workload e2e for maloja. Self-gates on a diff scoped to this workload (plus the
# shared library and the e2e machinery), delegating the matrix + run to the
# reusable e2e workflow.
name: E2E maloja
on:
  pull_request:
    branches: [main]
  workflow_dispatch:
permissions:
  contents: read
jobs:
  e2e:
    uses: ./.github/workflows/e2e.yml
    with:
      workload: maloja
      # yamllint disable-line rule:line-length
      paths: '^(workloads/maloja/|lib/|main\.libsonnet|hack/smoke/(lib|scenario-maloja)\.sh|\.github/workflows/e2e(-maloja)?\.yml)'
      scenario: hack/smoke/scenario-maloja.sh
      max_versions: '1'
