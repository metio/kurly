# SPDX-FileCopyrightText: The kurly Authors
# SPDX-License-Identifier: 0BSD

# generated by scripts/gen-smoke.sh — do not edit; regenerate with: gen-smoke
# Per-workload e2e for jellyseerr. Self-gates on a diff scoped to this workload (plus the
# shared library and the e2e machinery), delegating the matrix + run to the
# reusable e2e workflow.
name: E2E jellyseerr
on:
  pull_request:
    branches: [main]
    paths:
      - 'workloads/jellyseerr/**'
  workflow_dispatch:
permissions:
  contents: read
jobs:
  e2e:
    uses: ./.github/workflows/e2e.yml
    with:
      workload: jellyseerr
      paths: '^workloads/jellyseerr/'
      scenario: hack/smoke/scenario-jellyseerr.sh
      max_versions: '1'
