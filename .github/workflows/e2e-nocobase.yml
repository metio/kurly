# SPDX-FileCopyrightText: The kurly Authors
# SPDX-License-Identifier: 0BSD

# generated by scripts/gen-smoke.sh — do not edit; regenerate with: gen-smoke
# Per-workload e2e for nocobase. Self-gates on a diff scoped to this workload (plus the
# shared library and the e2e machinery), delegating the matrix + run to the
# reusable e2e workflow.
name: E2E nocobase
on:
  pull_request:
    branches: [main]
  workflow_dispatch:
permissions:
  contents: read
jobs:
  e2e:
    uses: ./.github/workflows/e2e.yml
    with:
      workload: nocobase
      # yamllint disable-line rule:line-length
      paths: '^(workloads/nocobase/|lib/|main\.libsonnet|hack/smoke/(lib|scenario-nocobase)\.sh|\.github/workflows/e2e(-nocobase)?\.yml)'
      scenario: hack/smoke/scenario-nocobase.sh
      max_versions: '1'
