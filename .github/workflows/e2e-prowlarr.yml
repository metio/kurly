# SPDX-FileCopyrightText: The kurly Authors
# SPDX-License-Identifier: 0BSD

# generated by scripts/gen-smoke.sh — do not edit; regenerate with: gen-smoke
# Per-workload e2e for prowlarr. Self-gates on a diff scoped to this workload (plus the
# shared library and the e2e machinery), delegating the matrix + run to the
# reusable e2e workflow.
name: E2E prowlarr
on:
  pull_request:
    branches: [main]
    paths:
      - 'workloads/prowlarr/**'
  workflow_dispatch:
permissions:
  contents: read
concurrency:
  group: e2e-prowlarr-${{ github.ref }}
  cancel-in-progress: true
jobs:
  e2e:
    uses: ./.github/workflows/e2e.yml
    with:
      workload: prowlarr
      paths: '^workloads/prowlarr/'
      scenario: hack/smoke/scenario-prowlarr.sh
      max_versions: '1'
