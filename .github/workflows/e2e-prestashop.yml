# SPDX-FileCopyrightText: The kurly Authors
# SPDX-License-Identifier: 0BSD

# generated by scripts/gen-smoke.sh — do not edit; regenerate with: gen-smoke
# Per-workload e2e for prestashop. Self-gates on a diff scoped to this workload (plus the
# shared library and the e2e machinery), delegating the matrix + run to the
# reusable e2e workflow.
name: E2E prestashop
on:
  pull_request:
    branches: [main]
    paths:
      - 'workloads/prestashop/**'
  workflow_dispatch:
permissions:
  contents: read
jobs:
  e2e:
    uses: ./.github/workflows/e2e.yml
    with:
      workload: prestashop
      paths: '^workloads/prestashop/'
      scenario: hack/smoke/scenario-prestashop.sh
      max_versions: '1'
