# SPDX-FileCopyrightText: The kurly Authors
# SPDX-License-Identifier: 0BSD

# generated by scripts/gen-smoke.sh — do not edit; regenerate with: gen-smoke
# Per-workload e2e for gatus. Self-gates on a diff scoped to this workload (plus the
# shared library and the e2e machinery), delegating the matrix + run to the
# reusable e2e workflow.
name: E2E gatus
on:
  pull_request:
    branches: [main]
    paths:
      - 'workloads/gatus/**'
  workflow_dispatch:
permissions:
  contents: read
jobs:
  e2e:
    uses: ./.github/workflows/e2e.yml
    with:
      workload: gatus
      paths: '^workloads/gatus/'
      scenario: hack/smoke/scenario-gatus.sh
      max_versions: '1'
