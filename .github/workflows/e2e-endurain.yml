# SPDX-FileCopyrightText: The kurly Authors
# SPDX-License-Identifier: 0BSD

# generated by scripts/gen-smoke.sh — do not edit; regenerate with: gen-smoke
# Per-workload e2e for endurain. Self-gates on a diff scoped to this workload (plus the
# shared library and the e2e machinery), delegating the matrix + run to the
# reusable e2e workflow.
name: E2E endurain
on:
  pull_request:
    branches: [main]
  workflow_dispatch:
permissions:
  contents: read
jobs:
  e2e:
    uses: ./.github/workflows/e2e.yml
    with:
      workload: endurain
      # yamllint disable-line rule:line-length
      paths: '^(workloads/endurain/|lib/|main\.libsonnet|hack/smoke/(lib|scenario-endurain)\.sh|\.github/workflows/e2e(-endurain)?\.yml)'
      scenario: hack/smoke/scenario-endurain.sh
      max_versions: '1'
