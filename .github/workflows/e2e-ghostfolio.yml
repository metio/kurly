# SPDX-FileCopyrightText: The kurly Authors
# SPDX-License-Identifier: 0BSD

# generated by scripts/gen-smoke.sh — do not edit; regenerate with: gen-smoke
# Per-workload e2e for ghostfolio. Self-gates on a diff scoped to this workload (plus the
# shared library and the e2e machinery), delegating the matrix + run to the
# reusable e2e workflow.
name: E2E ghostfolio
on:
  pull_request:
    branches: [main]
  workflow_dispatch:
permissions:
  contents: read
jobs:
  e2e:
    uses: ./.github/workflows/e2e.yml
    with:
      workload: ghostfolio
      # yamllint disable-line rule:line-length
      paths: '^(workloads/ghostfolio/|lib/|main\.libsonnet|hack/smoke/(lib|scenario-ghostfolio)\.sh|\.github/workflows/e2e(-ghostfolio)?\.yml)'
      scenario: hack/smoke/scenario-ghostfolio.sh
      max_versions: '1'
