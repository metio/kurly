# SPDX-FileCopyrightText: The kurly Authors
# SPDX-License-Identifier: 0BSD

# generated by scripts/gen-smoke.sh — do not edit; regenerate with: gen-smoke
# Per-workload e2e for overleaf. Self-gates on a diff scoped to this workload (plus the
# shared library and the e2e machinery), delegating the matrix + run to the
# reusable e2e workflow.
name: E2E overleaf
on:
  pull_request:
    branches: [main]
    paths:
      - 'workloads/overleaf/**'
  workflow_dispatch:
permissions:
  contents: read
jobs:
  e2e:
    uses: ./.github/workflows/e2e.yml
    with:
      workload: overleaf
      paths: '^workloads/overleaf/'
      scenario: hack/smoke/scenario-overleaf.sh
      max_versions: '1'
