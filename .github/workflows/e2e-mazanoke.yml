# SPDX-FileCopyrightText: The kurly Authors
# SPDX-License-Identifier: 0BSD

# generated by scripts/gen-smoke.sh — do not edit; regenerate with: gen-smoke
# Per-workload e2e for mazanoke. Self-gates on a diff scoped to this workload (plus the
# shared library and the e2e machinery), delegating the matrix + run to the
# reusable e2e workflow.
name: E2E mazanoke
on:
  pull_request:
    branches: [main]
    paths:
      - 'workloads/mazanoke/**'
  workflow_dispatch:
permissions:
  contents: read
concurrency:
  group: e2e-mazanoke-${{ github.ref }}
  cancel-in-progress: true
jobs:
  e2e:
    uses: ./.github/workflows/e2e.yml
    with:
      workload: mazanoke
      paths: '^workloads/mazanoke/'
      scenario: hack/smoke/scenario-mazanoke.sh
      max_versions: '1'
