# SPDX-FileCopyrightText: The kurly Authors
# SPDX-License-Identifier: 0BSD

# generated by scripts/gen-smoke.sh — do not edit; regenerate with: gen-smoke
# Per-workload e2e for radicale. Self-gates on a diff scoped to this workload (plus the
# shared library and the e2e machinery), delegating the matrix + run to the
# reusable e2e workflow.
name: E2E radicale
on:
  pull_request:
    branches: [main]
    paths:
      - 'workloads/radicale/**'
  workflow_dispatch:
permissions:
  contents: read
jobs:
  e2e:
    uses: ./.github/workflows/e2e.yml
    with:
      workload: radicale
      paths: '^workloads/radicale/'
      scenario: hack/smoke/scenario-radicale.sh
      max_versions: '1'
