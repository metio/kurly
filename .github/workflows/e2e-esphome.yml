# SPDX-FileCopyrightText: The kurly Authors
# SPDX-License-Identifier: 0BSD

# generated by scripts/gen-smoke.sh — do not edit; regenerate with: gen-smoke
# Per-workload e2e for esphome. Self-gates on a diff scoped to this workload (plus the
# shared library and the e2e machinery), delegating the matrix + run to the
# reusable e2e workflow.
name: E2E esphome
on:
  pull_request:
    branches: [main]
  workflow_dispatch:
permissions:
  contents: read
jobs:
  e2e:
    uses: ./.github/workflows/e2e.yml
    with:
      workload: esphome
      # yamllint disable-line rule:line-length
      paths: '^(workloads/esphome/|lib/|main\.libsonnet|hack/smoke/(lib|scenario-esphome)\.sh|\.github/workflows/e2e(-esphome)?\.yml)'
      scenario: hack/smoke/scenario-esphome.sh
      max_versions: '1'
