# SPDX-FileCopyrightText: The kurly Authors
# SPDX-License-Identifier: 0BSD

# generated by scripts/gen-smoke.sh — do not edit; regenerate with: gen-smoke
# Per-workload e2e for tachidesk. Self-gates on a diff scoped to this workload (plus the
# shared library and the e2e machinery), delegating the matrix + run to the
# reusable e2e workflow.
name: E2E tachidesk
on:
  pull_request:
    branches: [main]
    paths:
      - 'workloads/tachidesk/**'
  workflow_dispatch:
permissions:
  contents: read
jobs:
  e2e:
    uses: ./.github/workflows/e2e.yml
    with:
      workload: tachidesk
      paths: '^workloads/tachidesk/'
      scenario: hack/smoke/scenario-tachidesk.sh
      max_versions: '1'
