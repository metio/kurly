# SPDX-FileCopyrightText: The kurly Authors
# SPDX-License-Identifier: 0BSD

# generated by scripts/gen-smoke.sh — do not edit; regenerate with: gen-smoke
# Per-workload e2e for whoogle. Self-gates on a diff scoped to this workload (plus the
# shared library and the e2e machinery), delegating the matrix + run to the
# reusable e2e workflow.
name: E2E whoogle
on:
  pull_request:
    branches: [main]
  workflow_dispatch:
permissions:
  contents: read
jobs:
  e2e:
    uses: ./.github/workflows/e2e.yml
    with:
      workload: whoogle
      # yamllint disable-line rule:line-length
      paths: '^(workloads/whoogle/|lib/|main\.libsonnet|hack/smoke/(lib|scenario-whoogle)\.sh|\.github/workflows/e2e(-whoogle)?\.yml)'
      scenario: hack/smoke/scenario-whoogle.sh
      max_versions: '1'
