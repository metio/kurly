# SPDX-FileCopyrightText: The kurly Authors
# SPDX-License-Identifier: 0BSD

# generated by scripts/gen-smoke.sh — do not edit; regenerate with: gen-smoke
# Per-workload e2e for thanos. Self-gates on a diff scoped to this workload (plus the
# shared library and the e2e machinery), delegating the matrix + run to the
# reusable e2e workflow.
name: E2E thanos
on:
  pull_request:
    branches: [main]
  workflow_dispatch:
permissions:
  contents: read
jobs:
  e2e:
    uses: ./.github/workflows/e2e.yml
    with:
      workload: thanos
      # yamllint disable-line rule:line-length
      paths: '^(workloads/thanos/|lib/|main\.libsonnet|hack/smoke/(lib|scenario-thanos)\.sh|\.github/workflows/e2e(-thanos)?\.yml)'
      scenario: hack/smoke/scenario-thanos.sh
      max_versions: '1'
