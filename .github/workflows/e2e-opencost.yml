# SPDX-FileCopyrightText: The kurly Authors
# SPDX-License-Identifier: 0BSD

# generated by scripts/gen-smoke.sh — do not edit; regenerate with: gen-smoke
# Per-workload e2e for opencost. Self-gates on a diff scoped to this workload (plus the
# shared library and the e2e machinery), delegating the matrix + run to the
# reusable e2e workflow.
name: E2E opencost
on:
  pull_request:
    branches: [main]
    paths:
      - 'workloads/opencost/**'
  workflow_dispatch:
permissions:
  contents: read
jobs:
  e2e:
    uses: ./.github/workflows/e2e.yml
    with:
      workload: opencost
      paths: '^workloads/opencost/'
      scenario: hack/smoke/scenario-opencost.sh
      max_versions: '1'
