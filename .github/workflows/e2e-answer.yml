# SPDX-FileCopyrightText: The kurly Authors
# SPDX-License-Identifier: 0BSD

# generated by scripts/gen-smoke.sh — do not edit; regenerate with: gen-smoke
# Per-workload e2e for answer. Self-gates on a diff scoped to this workload (plus the
# shared library and the e2e machinery), delegating the matrix + run to the
# reusable e2e workflow.
name: E2E answer
on:
  pull_request:
    branches: [main]
  workflow_dispatch:
permissions:
  contents: read
jobs:
  e2e:
    uses: ./.github/workflows/e2e.yml
    with:
      workload: answer
      # yamllint disable-line rule:line-length
      paths: '^(workloads/answer/|lib/|main\.libsonnet|hack/smoke/(lib|scenario-answer)\.sh|\.github/workflows/e2e(-answer)?\.yml)'
      scenario: hack/smoke/scenario-answer.sh
      max_versions: '1'
