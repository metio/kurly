# SPDX-FileCopyrightText: The kurly Authors
# SPDX-License-Identifier: 0BSD

# generated by scripts/gen-smoke.sh — do not edit; regenerate with: gen-smoke
# Per-workload e2e for dokuwiki. Self-gates on a diff scoped to this workload (plus the
# shared library and the e2e machinery), delegating the matrix + run to the
# reusable e2e workflow.
name: E2E dokuwiki
on:
  pull_request:
    branches: [main]
  workflow_dispatch:
permissions:
  contents: read
jobs:
  e2e:
    uses: ./.github/workflows/e2e.yml
    with:
      workload: dokuwiki
      # yamllint disable-line rule:line-length
      paths: '^(workloads/dokuwiki/|lib/|main\.libsonnet|hack/smoke/(lib|scenario-dokuwiki)\.sh|\.github/workflows/e2e(-dokuwiki)?\.yml)'
      scenario: hack/smoke/scenario-dokuwiki.sh
      max_versions: '1'
