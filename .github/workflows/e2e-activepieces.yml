# SPDX-FileCopyrightText: The kurly Authors
# SPDX-License-Identifier: 0BSD

# generated by scripts/gen-smoke.sh — do not edit; regenerate with: gen-smoke
# Per-workload e2e for activepieces. Self-gates on a diff scoped to this workload (plus the
# shared library and the e2e machinery), delegating the matrix + run to the
# reusable e2e workflow.
name: E2E activepieces
on:
  pull_request:
    branches: [main]
  workflow_dispatch:
permissions:
  contents: read
jobs:
  e2e:
    uses: ./.github/workflows/e2e.yml
    with:
      workload: activepieces
      # yamllint disable-line rule:line-length
      paths: '^(workloads/activepieces/|lib/|main\.libsonnet|hack/smoke/(lib|scenario-activepieces)\.sh|\.github/workflows/e2e(-activepieces)?\.yml)'
      scenario: hack/smoke/scenario-activepieces.sh
      max_versions: '1'
